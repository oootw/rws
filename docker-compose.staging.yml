# Staging на VDS.
# Запуск:
#   bash scripts/init-env.sh staging        # создаёт .env (compose) и backend/.env
#   nano backend/.env                       # заполните секреты
#   make staging-up
#
# nginx наружу не публикуется — трафик принимает reverse-proxy (deploy/proxy).
# Стек целиком изолирован по COMPOSE_PROJECT_NAME=guard-staging.

services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
      target: production
    image: guard-reviews-app:staging
    environment:
      RUN_MIGRATIONS: "true"
      RUN_SEED: "false"

  worker:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
      target: production
    image: guard-reviews-app:staging
    environment:
      RUN_MIGRATIONS: "false"

  scheduler:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
      target: production
    image: guard-reviews-app:staging
    environment:
      RUN_MIGRATIONS: "false"

  nginx:
    build:
      context: .
      dockerfile: docker/nginx/Dockerfile
      args:
        VITE_ALLOW_TENANT_QUERY: "1"
    image: guard-reviews-nginx:staging
    container_name: ${COMPOSE_PROJECT_NAME:-guard-staging}-nginx
    networks:
      app: {}
      edge:
        aliases:
          - staging-nginx

networks:
  edge:
    external: true
    name: guard-edge
