# Админ-панель — план для следующей сессии

Документ-handoff. Цель: следующая сессия (в т. ч. с холодным контекстом)
должна продолжить с того места, где закончили, без перечитывания всего чата.

Связанные документы (читать сначала):
- `backend/саммари.md` — общая шпаргалка по backend и DDD-структуре проекта.
- `backend/docs/architecture.md` — правила слоёв, naming, тестирование.
- `bootstrap/providers.php` — порядок сервис-провайдеров.

---

## 1. Архитектурный контракт админки (фиксировано)

- **Стек:** Filament **v5.6** (`composer require filament/filament:^5.6`).
- **URL:** `/admin` на основном домене (см. `config/guardreviews.php` → `admin.path`).
- **Auth:** один супер-админ из `.env` (`ADMIN_EMAIL` + `ADMIN_PASSWORD_HASH`), отдельный гард `admin`, кастомный `UserProvider` (без таблицы `admin_users`).
- **DDD-граница:** Filament Resource = Interface-слой, такой же тонкий как HTTP-контроллеры.
  - **Чтение** (table/infolist) — напрямую через Eloquent (read-model паттерн).
  - **Запись** (create/edit/delete/custom actions) — **только через Application use cases**. Eloquent::save() в обход домена запрещён.
- **Имперсонация:** в Фазе 1 = выпуск Sanctum PAT с ability `impersonated` и TTL 15 мин. Plain-text токен показывается в Filament Notification один раз. Будущий Owner PWA подцепится через endpoint `POST /api/auth/exchange-impersonation-token` (он ещё не реализован — это задача Owner PWA).

---

## 2. Что уже сделано

### ✅ Фаза 0 — фундамент (выполнена)

| Файл | Назначение |
|---|---|
| `composer.json` | + `filament/filament:^5.6` |
| `app/Providers/Filament/AdminPanelProvider.php` | панель `/admin`, guard `admin`, RU-локаль |
| `app/Interface/Filament/Auth/AdminUser.php` | in-memory Authenticatable + FilamentUser/HasName/HasAvatar |
| `app/Interface/Filament/Auth/EnvUserProvider.php` | UserProvider читает из config |
| `app/Interface/Filament/Auth/AdminAuthServiceProvider.php` | регистрирует драйвер `env-admin` |
| `app/Interface/Console/Commands/SetAdminPasswordCommand.php` | `php artisan admin:password` |
| `config/auth.php` | guard `admin` + provider `env-admin` |
| `config/guardreviews.php` | секция `admin` (path/email/hash/name) |
| `.env.example` | `ADMIN_*` переменные |
| `phpunit.xml` | добавлен `APP_KEY` |

### ✅ Фаза 1 — Owners CRUD (выполнена)

**Domain (`app/Domain/Iam/Owner.php`)** — новые методы:
- `changeProfile(name, email, subdomain, telegramId, tariffId)` — полное обновление профиля.
- `overrideSubscription(?DateTimeImmutable)` — установить точную дату или сбросить.
- `changeTariff(?TariffId)` — сменить/отвязать тариф.
- `subdomainEquals(SubdomainSlug)` — для определения «реально переименован?».

**Domain (`OwnerRepository`)** — `delete(OwnerId): void` + Eloquent-реализация.

**Application — 5 новых use cases:**

| Use case | Папка |
|---|---|
| `UpdateOwnerProfile` | `app/Application/Iam/UpdateOwnerProfile/` |
| `DeleteOwner` | `app/Application/Iam/DeleteOwner/` |
| `OverrideSubscription` | `app/Application/Iam/OverrideSubscription/` |
| `ChangeOwnerTariff` | `app/Application/Iam/ChangeOwnerTariff/` |
| `IssueOwnerImpersonationToken` | `app/Application/Iam/IssueOwnerImpersonationToken/` (+ порт `OwnerImpersonationTokenIssuer`) |

+ Новое исключение: `App\Application\Iam\Exceptions\TariffNotFound`.

**Infrastructure:**
- `App\Infrastructure\Iam\SanctumOwnerImpersonationTokenIssuer` — адаптер порта.
- `App\Models\User` — добавлен trait `HasApiTokens` (Sanctum готов).
- Биндинг порт→адаптер в `IamServiceProvider`.

**Interface (Filament):**
- `app/Filament/Resources/Owners/OwnerResource.php` — корневой класс.
- `Schemas/OwnerForm.php` — create/edit форма.
- `Schemas/OwnerInfolist.php` — карточка просмотра.
- `Tables/OwnersTable.php` — список с фильтрами (тариф + TernaryFilter подписки).
- `Actions/OwnerActionFactory.php` — 4 action'а: `extend_subscription`, `override_subscription`, `change_tariff`, `impersonate`.
- `Pages/CreateOwner.php` + `EditOwner.php` — `handleRecordCreation` / `handleRecordUpdate` проксируют в use cases.
- `resources/views/filament/notifications/impersonation-token.blade.php` — `<pre>` для plain-text токена.

**Тесты — 60+ новых:**
- `tests/Unit/Domain/Iam/OwnerTest.php` (12 тестов).
- `tests/Unit/Interface/Filament/Auth/AdminUserTest.php` (11).
- `tests/Unit/Interface/Filament/Auth/EnvUserProviderTest.php` (11).
- `tests/Unit/Application/Iam/UpdateOwnerProfileHandlerTest.php` (5).
- `tests/Unit/Application/Iam/DeleteOwnerHandlerTest.php` (2).
- `tests/Unit/Application/Iam/OverrideSubscriptionHandlerTest.php` (3).
- `tests/Unit/Application/Iam/ChangeOwnerTariffHandlerTest.php` (4).
- `tests/Unit/Application/Iam/IssueOwnerImpersonationTokenHandlerTest.php` (3).
- `tests/Feature/Admin/AdminLoginTest.php` (6).
- `tests/Feature/Admin/OwnerResourceTest.php` (17).
- `tests/Feature/Admin/OwnerResourceFiltersTest.php` (5 — Livewire).
- `tests/Feature/Admin/AdminSecurityTest.php` (7).
- `tests/Feature/Admin/SetAdminPasswordCommandTest.php` (6).
- `tests/Feature/Admin/SanctumImpersonationTokenIssuerTest.php` (4).

**Итог:** `php artisan test` → **125 passed, 0 failed, 587 assertions**.

---

## 3. Подводные камни (узнаны на горьком опыте — не наступать заново)

### 3.1. Окружение разработки

- **Локальный PHP 8.2 на хосте**, проект требует **8.4**. Все команды (`composer`, `artisan`, тесты) запускать в Docker:
  ```bash
  docker run --rm -v /Users/rasa/dev/rws/backend:/app -w /app php:8.4-cli-alpine sh -c \
    "apk add --no-cache icu-libs icu-dev libzip-dev zip > /dev/null 2>&1 && \
     docker-php-ext-install intl > /dev/null 2>&1 && \
     php artisan test 2>&1"
  ```
- Для `composer require` можно с хоста: `composer require <pkg> --ignore-platform-req=php --ignore-platform-req=ext-intl` (composer обновит lock, реальный install через `composer install` пойдёт в контейнере при сборке Docker-образа).
- **Warnings `file_get_contents(/app/.env): Failed to open stream`** — общесистемный шум при одноразовых `docker run` (нет `.env` в контейнере). Тесты от этого не падают; assertions работают.

### 3.2. Filament v5 — особенности

- **Filament v3 НЕ работает с Laravel 13** (`illuminate/auth ^10|^11|^12`). Используем v5.x — он поддерживает Laravel 13 (`illuminate/contracts: ^11.28|^12.0|^13.0`).
- **Авто-discovery resources/pages/widgets** — в `AdminPanelProvider->panel()` указаны пути; вложенные папки сканируются рекурсивно.
- **`$model = User::class`** в Resource. Хотя домен — `App\Domain\Iam\Owner`, для Filament читателя выступает Eloquent-модель.
- **Generator команды** `php artisan make:filament-resource` создаёт ресурс с глубокой структурой: `OwnerResource.php` + `Tables/OwnersTable.php` + `Schemas/OwnerForm.php` + `Schemas/OwnerInfolist.php` + `Pages/{List,Create,View,Edit}.php`. Мы перезаписываем сгенерённые файлы.

### 3.3. Кастомный гард `admin` — заглушки в `AdminUser`

`AdminUser extends GenericUser` нужен набор «Eloquent-подобных» методов, которых GenericUser не имеет — но Filament/Livewire зовут их в blade-view:
- `getKey(): string` — иначе `notifications.blade.php` падает.
- `getAttributeValue(string $key): mixed` — иначе `avatar/user.blade.php` падает.
- `getFilamentName()` + `getFilamentAvatarUrl()` через `HasName`/`HasAvatar` интерфейсы.
- `getRememberToken(): ?string` + `setRememberToken($v): void` — overriden чтобы не получать `Undefined array key "remember_token"` при logout.

### 3.4. Тесты Livewire-компонентов Filament

Чтобы тестировать таблицы/фильтры через `Livewire::test(ListOwners::class)`, нужно:
```php
use Filament\Facades\Filament;

beforeEach(function (): void {
    // ... actingAs admin ...
    Filament::setCurrentPanel('admin');
    Filament::setServingStatus(true);
});
```
Иначе `NoDefaultPanelSetException`.

### 3.5. Что НЕ работает в Filament v5 (грабли проверены)

- **`Action::make('reset_filters')->visible(false)` как placeholder в `toolbarActions`** — Filament всё равно пытается зарезолвить и падает. Если actions нет — передавай пустой массив `->toolbarActions([])`.
- **TernaryFilter с `where(closure)` вложенным** — падает `Call to a member function newQueryWithoutRelationships() on null`. Использовать плоскую форму:
  ```php
  ->queries(
      true: fn (Builder $q) => $q->where('subscription_ends_at', '>', now()),
      false: fn (Builder $q) => $q->whereNull('subscription_ends_at')
          ->orWhere('subscription_ends_at', '<=', now()),
  )
  ```
- **`->modifyQueryUsing(fn (Builder $q) => $q->with('tariff'))`** — лишний. Filament сам определяет нужный `with` из колонки `tariff.title`.

### 3.6. PaymentTransaction migration

Таблица `payment_transactions` имеет:
- `user_id` → `users.id` (CASCADE on delete) — каскад работает.
- `tariff_id` → `tariffs.id` (**restrict on delete**) — обязательно нужен при создании.

В тестах: при создании транзакции для проверки каскада удаления Owner — обязательно передавать `tariff_id`.

### 3.7. `phpunit.xml` APP_KEY

Без `APP_KEY` Filament падает в `EncryptCookies`. Зафиксировано тестовое значение:
```xml
<env name="APP_KEY" value="base64:AckfSECXIvnK5r28GVIWUAxmbBSjTsmF11WBzy3uoaA="/>
```

---

## 4. Конфигурация `.env` (для входа в админку)

```env
ADMIN_PANEL_PATH=admin           # необязательно — дефолт 'admin'
ADMIN_EMAIL=dev@guardreviews.local
ADMIN_PASSWORD_HASH=             # см. ниже
ADMIN_NAME=Developer
```

Генерация хеша:
```bash
php artisan admin:password
# или (готовая строка для .env):
php artisan admin:password --show-env
```
После обновления `.env`: `php artisan config:clear`.

---

## 5. Следующие фазы (в порядке)

### 🟦 Фаза 2 — Places (CRUD + активация + QR)

**Use cases (новые):**
- `UpdatePlace` (title, platforms, backgroundImageUrl).
- `ChangePlaceActivation` (activate/deactivate).
- `DeletePlace` (каскад на reviews/action_logs через FK).

**Domain:**
- Методы в `Place`: `updateProfile()`, `activate()`, `deactivate()`.
- `PlaceRepository::delete(PlaceId)`.

**Filament Resource:**
- `app/Filament/Resources/Places/PlaceResource.php` + Pages/Schemas/Tables/Actions.
- `table()`: title, owner (filter), is_active (toggle), кол-во платформ, кол-во отзывов, created_at.
- `form()`: title, owner_id (select), background_image_url, **repeater для `platforms`** (type enum + url + label).
- Action `preview_qr` — модалка с PNG/SVG через существующий `App\Services\QrCodeService`.
- Action `download_qr` — отдача файла.
- Bulk-action: активация/деактивация пачкой.

**Тесты:**
- Unit на новые handlers (4-5 файлов).
- Feature на ресурс: list/CRUD/QR-генерация/каскад при удалении.

### 🟩 Фаза 3 — Reviews (просмотр + статусы + удаление)

**Use cases:**
- ✅ `ChangeReviewStatus` — есть.
- 🆕 `DeleteReview` — для спама. Пишет в ActionLog (новый `ActionType::AdminDeletedReview`).
- 🆕 (опц.) `ResendNegativeReviewAlert` — `SendNegativeReviewAlert::dispatch($id)`.

**Resource:** только view + edit-status + delete. **Без формы create** — отзывы создаёт только посетитель.

**Колонки:** stars (бейдж со звёздами), text (truncate), contact, place (relation), owner (через place), status (badge), created_at. Фильтры: статус, place_id, owner_id, диапазон дат.

### 🟨 Фаза 4 — Payments + Tariffs

**4.1. Tariffs** — полный CRUD напрямую через Eloquent (конфигурационная сущность, без сложных инвариантов). НО: 🆕 use case `SetDefaultTariff` — гарантирует инвариант «ровно один is_default» на уровне Application.

**4.2. Payments** — read-only + 2 action'а:
- `Action::make('refire_webhook')` — симуляция Tinkoff webhook (debug-only, через `HandlePaymentNotificationHandler`). **Только не-production env**, защитить через `->visible(fn () => !app()->isProduction())`.
- `Action::make('mark_failed')` — для зависших pending: 🆕 `ForceFailPaymentHandler`.

### 🟥 Фаза 5 — операционная панель

Не Resource'ы, а custom Pages.

- **ActionLogs Resource** — read-only с фильтрами + widget на dashboard (график сканов/редиректов/негатива за неделю через `WeeklySummaryReader`).
- **Notifications Log** — 🆕 миграция `notification_deliveries` (owner_id, channel, kind, status, error). Записывается из `MultiChannelOwnerNotifier` (добавить порт `NotificationDeliveryLogger` + Eloquent-адаптер).
- **`FailedJobsPage`** — кастомная страница, читает таблицу `failed_jobs` напрямую. Actions: retry/delete/prune.
- **`EnvViewerPage`** — read-only, читает `base_path('.env')` через `EnvReaderService` + `EnvMasker` (regex `KEY|SECRET|TOKEN|PASSWORD|HASH|SALT|DSN` → `••• (length: N)`).
- **`TelegramBotPage`** — `Nutgram::getMe()` + `getWebhookInfo()` (cached 30s). Actions: `set_webhook`, `delete_webhook`, `send_test_message`.
- **Dashboard widgets:** OwnersStats, ReviewsStats, RecentReviews, FailedJobsCount.

### 🟪 Фаза 6 — полировка и безопасность

- **Аудит admin-действий.** Миграция `admin_action_logs` (action, payload jsonb, ip, ua, created_at). Глобальный `Action::configureUsing(...)` пишет каждое действие.
- **Защита edge.** Caddy basic-auth поверх Filament на staging/prod. `THROTTLE_ADMIN=60,1`. Опц. IP allow-list через env `ADMIN_ALLOWED_IPS`.
- **Production safeguards.** `refire_webhook`, `send_test_message` etc — `->visible(fn () => !app()->isProduction())`. `->requiresConfirmation()` на destructive.
- **Локализация.** Подключить `lang/ru` для Filament + наши строки.
- **Документация.** `backend/docs/admin.md` (как добавить нового админа, новый Resource, новый action с use case). Обновить `саммари.md`.
- **CI.** `tests/Feature/Admin/` в `composer test`, прогнать Pint.

---

## 6. Готовые команды (cheatsheet)

```bash
# Запуск всех тестов в Docker (PHP 8.4)
docker run --rm -v /Users/rasa/dev/rws/backend:/app -w /app php:8.4-cli-alpine sh -c \
  "apk add --no-cache icu-libs icu-dev libzip-dev zip > /dev/null 2>&1 && \
   docker-php-ext-install intl > /dev/null 2>&1 && \
   php artisan test 2>&1"

# Только admin-тесты
... php artisan test tests/Feature/Admin tests/Unit/Application/Iam tests/Unit/Interface/Filament

# Pint форматирование (можно с хоста, не нужен Docker)
./vendor/bin/pint app/Filament app/Application/Iam app/Domain/Iam tests/Feature/Admin tests/Unit/Interface

# Маршруты админки
docker run ... php artisan route:list --path=admin

# Сгенерить новый Filament Resource
docker run ... php artisan make:filament-resource Place --view --no-interaction
# Создаст app/Filament/Resources/Places/{PlaceResource,Pages/,Schemas/,Tables/}.

# Создать админский пароль локально
docker run ... php artisan admin:password
```

---

## 7. Чек-лист «добавить новую фичу в админке»

Всякий раз когда добавляешь новый Resource или action:

1. **Domain.** Если новые мутации — добавить методы в агрегат + расширить Repository (`delete`/`findAll`/…).
2. **Application.** Создать папку `<UseCase>/` с `Command` + `Handler` (+ `Result`/`View` при необходимости).
3. **Infrastructure.** Если новый порт — реализовать адаптер + bind в `<Context>ServiceProvider`.
4. **Filament Resource.**
   - `$model = <EloquentModel>::class`.
   - `table()/form()/infolist()` — конфиг.
   - `Actions/<Context>ActionFactory.php` — custom actions.
   - `Pages/Create*.php` + `Edit*.php` — переопределить `handleRecordCreation` / `handleRecordUpdate` → use case.
   - `DeleteAction::make()->action(fn ($record) => app(DeleteHandler::class)->handle(...))`.
5. **Тесты.**
   - Unit на handlers (+ Domain, если методы добавлены).
   - Feature на Resource:
     - HTTP smoke (list/create/view/edit).
     - Livewire-фильтры (через `Livewire::test(ListPage::class)`).
     - Интеграция handlers поверх Eloquent (`RefreshDatabase`).
     - Edge cases: cascade-delete, idempotency, validation errors.
6. **Pint.** `./vendor/bin/pint <new-files>`.
7. **Проверь** что все 4 слоя на месте: Domain нет `use Illuminate\…`, FQ-классы в Application импортированы, контроллеры/actions тонкие.

---

## 8. Открытые вопросы (решить в начале след. сессии)

- **Owner PWA** ещё не создан → endpoint `POST /api/auth/exchange-impersonation-token` не нужен пока. В Фазе 1 имперсонация = выпуск + копирование plain-text. Если PWA появится — это отдельная задача (Sanctum проверка + установка cookie + редирект).
- **Bulk actions** для Owners намеренно не сделаны (можно случайно удалить всех). Если нужны — добавить bulk-extend/bulk-tariff-change в Фазе 6.
- **`max_id`** — отображается в форме, но disabled (привязывается через бот командой `/link`). Если PWA сделает свою привязку — обновить.
- **TLS на проде** для `/admin` — должен быть закрыт двумя слоями (Caddy basic-auth + Filament login). Сейчас только Filament — это OK для staging, для prod добавить Caddy в Фазе 6.

---

## 9. Текущее состояние тестов

```
Tests:    169 warnings, 125 passed (587 assertions)
Duration: ~3s
```

Warnings — это глобальный шум `file_get_contents(/app/.env): Failed to open stream` от dotenv при `docker run`-запусках. Все assertions проходят, failures = 0. В CI на CI-runner'е `.env` будет, и warnings исчезнут.
