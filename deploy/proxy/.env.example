# Конфиг edge-прокси (Caddy).
# Скопируйте: cp deploy/proxy/.env.example deploy/proxy/.env

# Базовый домен production-стенда
DOMAIN=otziv.space

# Базовый домен staging-стенда
STAGING_DOMAIN=staging.otziv.space

# E-mail для Let's Encrypt (обязателен, иначе ACME отклонит запросы)
ACME_EMAIL=admin@otziv.space
